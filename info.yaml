# Tiny Tapeout project information
project:
  title:        "68k Sound Chip"           # Project title
  author:       "Ben Payne"                # Your name
  discord:      "interlude_audio_ben"      # Your discord username, for communication and automatically assigning you a Tapeout role (optional)
  description:  "PSG sound chip (PWM/I2S/S-PDIF outputs) for a 68k-based retrocomputer"   # One line description of what your project does
  language:     "Verilog"                  # other examples include SystemVerilog, Amaranth, VHDL, etc
  clock_hz:     24576000                   # 24.576 MHz - chosen so 24.576MHz/512 = 48kHz exactly; see docs/design/sound-chip.md #4

  # How many tiles your design occupies? A single tile is about 167x108 uM.
  tiles: "1x1"          # Valid values: 1x1, 1x2, 2x2, 3x2, 4x2, 6x2 or 8x2

  # Your top module name must start with "tt_um_". Make it unique by including your github username:
  top_module:  "tt_um_benpayne_sound_chip"

  # List your project's source files here.
  # Source files must be in ./src and you must list each source file separately, one per line.
  # Don't forget to also update `PROJECT_SOURCES` in test/Makefile.
  source_files:
    - "project.v"
    - "psg_timebase.v"
    - "bus68k_if.v"
    - "psg_regs.v"
    - "psg_tone.v"
    - "psg_noise.v"
    - "psg_mixer.v"
    - "pwm_out.v"
    - "i2s_out.v"
    - "spdif_out.v"

# The pinout of your project. Leave unused pins blank. DO NOT delete or add any pins.
# This section is for the datasheet/website. Use descriptive names (e.g., RX, TX, MOSI, SCL, SEG_A, etc.).
# See docs/design/sound-chip.md #2 and docs/design/68k-bus-interface.md #3 for the rationale.
pinout:
  # Inputs - 68k bus control/address (68008-style, single byte lane)
  ui[0]: "cs_n"
  ui[1]: "as_n"
  ui[2]: "r_w"
  ui[3]: "ds_n"
  ui[4]: "a1"
  ui[5]: "a2"
  ui[6]: "a3"
  ui[7]: ""

  # Outputs
  uo[0]: "dtack_n"
  uo[1]: "pwm_out"
  uo[2]: "i2s_bclk"
  uo[3]: "i2s_lrclk"
  uo[4]: "i2s_sdata"
  uo[5]: "spdif_out"
  uo[6]: ""
  uo[7]: ""

  # Bidirectional pins - 68k data bus D0-D7
  uio[0]: "d0"
  uio[1]: "d1"
  uio[2]: "d2"
  uio[3]: "d3"
  uio[4]: "d4"
  uio[5]: "d5"
  uio[6]: "d6"
  uio[7]: "d7"

# Do not change!
yaml_version: 6
